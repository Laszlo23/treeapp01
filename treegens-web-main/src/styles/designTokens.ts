/** Shared visual constants — pair with `.tg-*` helpers in `globals.css`. */
export const tg = {
  radiusCard: 'rounded-3xl',
  shadowCard: 'shadow-[0_12px_40px_rgba(30,15,8,0.08)]',
  transition: 'transition-all duration-200 ease-out',
  press: 'active:scale-[0.98]',
} as const
